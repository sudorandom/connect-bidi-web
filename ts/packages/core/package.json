{
  "name": "@sudorandom/connect-bidi-core",
  "version": "0.3.0",
  "description": "Transport-agnostic bridge between a bidirectional byte socket (WebSocket, WebTransport) and ConnectRPC server handlers",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sudorandom/connect-bidi-web.git",
    "directory": "ts/packages/core"
  },
  "scripts": {
    "generate": "buf generate ../../../proto",
    "prebuild": "rm -rf ./dist/*",
    "build": "tsc --project tsconfig.build.json --outDir ./dist",
    "test": "tsc --noEmit && node --import tsx --test $(find src -name '*.spec.ts')",
    "format": "biome format --write .",
    "lint": "biome lint --error-on-warnings .",
    "attw": "attw --pack --profile esm-only"
  },
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "peerDependencies": {
    "@bufbuild/protobuf": "^2.7.0",
    "@connectrpc/connect": "^2.1.0"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.72.0",
    "@bufbuild/protoc-gen-es": "^2.13.0",
    "@types/node": "^26.1.2",
    "tsx": "^4.20.6"
  }
}
